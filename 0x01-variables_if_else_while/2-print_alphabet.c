#include <stdio.h>

/**
* main - Entry pointk
*
* Description: This program prints the alphabet in lowercce
* Return: Always 0 (Success)
**/

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; ++a)
		putchar(a);
		putchar('\n');

		return (0);
}

